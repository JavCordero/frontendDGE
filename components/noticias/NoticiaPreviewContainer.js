import styled from "@emotion/styled";

/*
  componente que contiene las vistas previas de las noticias
*/

const NoticiaPreviewContainer = styled.div`
  display: grid;
  grid-template-columns: 100%;
  grid-template-rows: auto;
`;

export default NoticiaPreviewContainer;
